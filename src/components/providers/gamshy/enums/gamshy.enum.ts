export enum CallbackAction {
    LOGIN = "login",
    WITHDRAW = "withdraw",
    DEPOSIT = "deposit",
    LOGOUT = "logout",
    ROLLBACK = "rollback-withdrawal",
    GETBALANCE = "get-balance"
}

export enum BetType {
    BET = "bet",
    TIP = "tip",
    FREESPIN = "freespin"
}
