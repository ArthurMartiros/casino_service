export enum CallbackAction {
    BALANCE = "balance",
    BET = "bet",
    WIN = "win",
    REFUND = "refund",
    ROLLBACK = "rollback"
}

export enum BetType {
    BET = "bet",
    TIP = "tip",
    FREESPIN = "freespin"
}
